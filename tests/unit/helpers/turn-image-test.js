import { turnImage } from 'checkers/helpers/turn-image';
import { module, test } from 'qunit';

module('Unit | Helper | turn image');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = turnImage([42]);
  assert.ok(result);
});
