import { soundBackground } from 'checkers/helpers/sound-background';
import { module, test } from 'qunit';

module('Unit | Helper | sound background');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = soundBackground([42]);
  assert.ok(result);
});
