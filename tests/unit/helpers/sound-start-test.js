import { soundStart } from 'checkers/helpers/sound-start';
import { module, test } from 'qunit';

module('Unit | Helper | sound start');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = soundStart([42]);
  assert.ok(result);
});
